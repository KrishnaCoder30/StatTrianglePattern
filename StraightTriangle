#include <stdio.h>
 
int main()
{
 int a,b,c;
 scanf("%d",&c);
 for(a=0;a<c;a++)
 {
 	for(b=c-2-a;b>=0;b--)
 	{
 		printf(" ");
 		}
 	for(b=2*a;b>=0;b--)
 	{
 		printf("*");
 		}
 		printf("\n");
 		}
}
 
